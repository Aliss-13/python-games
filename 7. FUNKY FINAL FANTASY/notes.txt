Séparer en plusieurs fichiers permet de rendre le code : 
-plus lisible 
-plus organisé : chaque fichier (module) a un rôle (combat, données, affichage...)
-plus facile à modifier sans tout casser : on peut en modifier une partie plus facilement

data.py contient uniquement les données du jeu (objets, effets, constantes). 
Il ne contient pas de logique, seulement des informations utilisées par les autres modules.
Il permet de séparer clairement les données et le comportement du programme.
data.py = source de vérité

L'affichage, les données et la logique du combat doivent être indépendants pour que chaque partie du programme ait un rôle clair.
data.py : qu'est ce qui existe ?
combat.py : que se passe t-il ?
affichage.py : comment c'est montré ?

L'inventaire stocke uniquement un identifiant d'objet, afin de récupérer ses propriétés dans data.objets.
Cela évite de dupliquer les données et rend le système plus flexible.

item_id : le nom/identifiant de l'objet (ex:"plume_phenix") = CLE
data.objets[item_id] : le dictionnaire complet de l'objet stocké dans data.py = VERITE DES DONNEES
item : = COPIE LOGIQUE UTILISEE DANS LE CODE
-la variable qui contient ce dictionnaire récupéré depuis data.objets
-le résultat de la recherche dans data.objets

inventaire.pop(index) supprime un élément de la liste à la position indiquée par "index".
index doit être un entier car les éléments d'une liste sont repérés par leur position numérique.

combat => ordre initiative => tri des entités par vitesse => boucle des tours

vivants = get_vivants(equipe)
on filtre les personnages avec vie > 0 pour éviter que des personnages morts puissent agir, être ciblés ou provoquer 
des erreurs dans les boucles de combat.

Les effets sont des états appliqués à un personnage/plusieurs personnages qui persistent dans le temps. Ils sont stockés dans
le dictionnaire du personnage sous forme de dictionnaires avec tous leurs paramètres (durée en tours, dégâts, soins ...).
Ils sont réévalués à chaque tour afin que leur action soit appliquée tant que leur durée n'est pas terminée.

get_item(inventaire, choix) transforme un index d'inventaire en objet exploitable en récupérant d'abord l'identifiant de la liste,
puis les données correspondantes dans data.objets
index (choix joueur) => inventaire[index] => item_id => data.objets[item_id] => item utilisable

Un tuple est une structure de données immuable qui permet de stocker plusieurs valeurs ensemble.
return (item_id, data.objets[item_id]) => tuple : c'est comme une boîte avec [ item_id | item_data ] dedans.
item_id, item = get.item() => on déballe le tuple : 
    1ère valeur : item_id (ex : "robe_brodee")
    2e valeur : item_data (ex : {"type" : "equipement", "classe" : ["magie"]....})

TypeError : utilisation d'un type de données incompatible avec l'opération effectuée.
ex : "5" + 3 = (impossible car str + int)
inventaire['item'] => si inventaire est une liste : erreur

KeyError : la clé demandée n'existe pas dans le dictionnaire
ex : d = {"a" : 1}
print(d["b"]) => KeyError 'b'
data_objets[item_id]
si item_id = None
data_objets[none] renvoie KeyError: None

Chaîne de caractères : suite de caractères (lettres, chiffres, symboles) utilisée pour représenter du texte. 
(ex : "Jupiter123**")
La chaîne de caractères est immuable, c'est à dire qu'on ne peut pas changer la suite de caractères.

Dictionnaire : structure de données qui associe des clés uniques à des valeurs
(ex : {"nom" : "Marjane", "numero" : "06.25.34.35.52", "courriel" : "marjanelala@"})
L'accès se fait par clé et non par position.

Liste : collection ordonnée d'éléments modifiables, accessibles par leur index.
(ex : ["Marjane", "R2D2", "4"])
Elle est ordonnée, indexée (0, 1, 2...) et modifiable.

l'inventaire est une structure dynamique (liste) qui évolue selon les actions du joueur :
-ajout via loot
-retrait lors de la consommation ou de l'équipement
-retour d'objets lors du remplacement d'équipements


# {skill.name:<25}

        # skill.name → la valeur à afficher.
        # : → début des options de formatage.
        # < → alignement à gauche (left).
        # 25 → largeur minimale de 25 caractères.

        # Résultat :
        # 1. Étincelle                Coût : 0
        # 2. Boule de feu             Coût : 10
        # 3. Explosion pyrotechnique  Coût : 15


        # f"{skill.name:>25}"
                        # Étincelle
                    # Boule de feu
        # Explosion pyrotechnique


        # f"{skill.name:^25}"
            # Étincelle
            # Boule de feu
        # Explosion pyrotechnique



TEMPORARY NOTES

from enum import Enum

class Target(Enum):
    SELF = "self"
    OPPONENT = "opponent"
    ALL_OPPONENTS = "all_opponents"
    ALLY = "ally"
    ALL_ALLIES = "all_allies"

    skill.target = Target.ALL_OPPONENTS


------------------------------------progression de zone

Découverte de symbole étrange
+1 progression

Trouver une cabane abandonnée
+2 progression

Vaincre un ennemi rare
+1 progression

zone.progress >= 10
zone.sub_boss_unlocked = True



Et là, les quêtes deviennent naturelles

Par exemple dans la forêt :

Quête 1 : Les traces dans les bois

Objectif :

trouver 3 indices

Événements :

arbre étrange
empreinte
corbeau parlant

Récompense :

accès au sous-boss
Quête 2 : Les chasseurs disparus

Objectif :

vaincre des loups
retrouver un objet

Récompense :

équipement rare
Quête 3 : La cabane de la sorcière

Objectif :

récupérer trois ingrédients

Débloque :

Boss : The VVitch



══════════════════════
      Forêt Maudite
══════════════════════

1. Explorer
2. Équipe
3. Inventaire
4. Sauvegarder
0. Quitter

Types d'events:

combat
discovery
loot
quest
merchant
rest
trap
nothing


début
40 % combat
25 % découverte
15 % loot
10 % rien
10 % repos

milieu
60 % combat
20 % découverte
10 % piège
10 % repos




------------------------------------idées zones suivantes
    Zone(
        id="burning_desert",
        name="Désert brûlant",

        recommended_level=10,
        unlock_requirement="the_vvitch",

        enemies=["coyote", "vulture", "skeleton"],

        sub_boss="the_road"

        boss="the_hitcher"
    ),

    Zone(
        id="steep_mountain",
        name="Montagne abrupte",

        recommended_level=10,
        unlock_requirement="the_vvitch",

        enemies=["eagle", "wolf", "stone_elemental"],

        sub_boss="big_foot"
        boss="missing_411"
    )

]

En Python :

= sert à attribuer une valeur
== sert à comparer


item_id = "phoenix_feather"

➡️ une clé dans un dictionnaire.

item = ITEMS["phoenix_feather"]

➡️ le vrai objet avec ses propriétés.

TODO :
- séparer gestion équipement / consommable / matériau si le système d'objets grossit




Ou carrément dans main.py

Juste avant le combat :

enemy_team = [
    create_opponent("black_phillip")
]

Puis :

combat(player_team, enemy_team, inventory, current_zone)

Là tu lances directement le combat sans dépendre des événements.


menu_zone(character, player_team, enemy_team, inventory, current_zone):





Type	Attaque normale	Effets	Compétences
Commun	        ✅	    ❌	    ❌
Inhabituel	    ✅	    ✅	    ❌
Rare	        ✅	    ✅ (pls)	❌
Sous-boss	    ❌	   via compétences	✅
Boss	        ❌	    via compétences	✅




Là tu as deux contextes différents :

CombatContext

Le plateau :

joueurs
ennemis


SkillContext

L'action en cours :

qui lance ?
quelle compétence ?
qui est ciblé ?



GENERATION D'UN LOOT

Combat gagné
      |
      v
generate_loot(enemy, zone)
      |
      |
      +--> profil ennemi
      |       |
      |       +--> drop_rate
      |       +--> nombre d'objets
      |       +--> rareté possible
      |
      v
choose_rarity()
      |
      v
FOREST_LOOTS[rarity]
      |
      v
choix de l'item
      |
      v
create_loot_item()
      |
      +--> récupère ITEMS[item_id]
      |
      +--> tire item_level
      |
      +--> si équipement
      |       |
      |       v
      |   scale_item_bonus()
      |
      v
loot final ajouté à l'inventaire




Ce qui appartient à GameState

Tout ce qui survit après le combat :

personnages
XP
niveaux
inventaire
zone actuelle
progression
quêtes
boss vaincus

Donc :

SaveManager.save(game)

est parfait.

Ce qui appartient à CombatContext

Tout ce qui existe uniquement pendant un combat :

ordre des tours
ennemi actuel
cible sélectionnée
effets temporaires
buffs/debuffs
historique du combat



Ce serait bien de retenir : 

_____.get(bidule) c'est pour un dictionnaire

for flag in zone.flags:

    event = FOREST_EVENTS.get(flag)

    if event:
        print(f"- {event.name}")

______fonction get_bidule_by_id(bidule_id) c'est pour une liste

for enemy_id, count in zone.enemy_kills.items():

    enemy = get_enemy_by_id(enemy_id) # récupère l'objet "ennemi" grâce à son id.

        print(f"- {enemy.name} : {count}")

def get_enemy_by_id(enemy_id, enemy_list):

    for enemy in enemy_list:
        if enemy.id == enemy_id:
            return enemy

    raise ValueError(f"Ennemi inconnu : {enemy_id}")

Dictionnaire = placard avec étiquettes → j'ouvre directement le tiroir.
Liste = tas d'objets → je fouille.


J'ai...	                    Je fais...
dict avec une clé	        >  .get(clé)
list d'objets avec un id	>  get_xxx_by_id()
list simple	                >  boucle for ou index [0]
objet avec attribut	        >  .attribut

